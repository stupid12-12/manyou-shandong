services:
  mysql:
    image: mysql:8.4
    environment:
      MYSQL_DATABASE: manyou_shandong
      MYSQL_USER: manyou
      MYSQL_PASSWORD: manyou
      MYSQL_ROOT_PASSWORD: root
    ports:
      - "3306:3306"
    volumes:
      - mysql-data:/var/lib/mysql
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "localhost", "-proot"]
      interval: 5s
      timeout: 3s
      retries: 20
volumes:
  mysql-data:
